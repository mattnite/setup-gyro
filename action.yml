name: 'Setup gyro'
description: 'Install gyro, a zig package manager'
branding:
  icon: truck
  color: orange
inputs:
  version:
    description: 'gyro version to install'
    required: true
    default: 'latest'
author: mattnite
runs:
  using: 'node12'
  main: 'dist/index.js'
