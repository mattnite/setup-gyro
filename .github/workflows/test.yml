name: "units-test"
on:
  pull_request:
  push:
    branches:
      - main
      - 'releases/*'

jobs:
  test:
    runs-on: [ubuntu-latest, windows-latest]
    steps:
    - uses: actions/checkout@v2
    - uses: ./
    - run: gyro add gyro/version
